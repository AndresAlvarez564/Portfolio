# TK-34 | Implement admin Dashboard page

## Type
Feature

## Priority
Medium

## Status
Ready for Review

## Branch
dev

## Merge Requirements
- [ ] All tasks completed and tested locally
- [x] No secrets or credentials committed
- [ ] Tested in dev environment
- [ ] PR reviewed and approved
- [x] No regressions in existing functionality

## Depends On
- TK-25 (Feature: projects module - GET /projects/admin)
- TK-30 (Feature: contact module - GET /contact)

## Objective
Implement the admin Dashboard page that gives Andres a quick overview of the portfolio content status: project counts, unread messages, and quick navigation links to each admin section.

## Scope
- Frontend: Dashboard page with stat cards and quick links
- No new backend routes needed - uses existing admin endpoints

## Flow

```text
Admin navigates to /admin/dashboard
Dashboard fires parallel API calls:
- GET /projects/admin -> project stats
- GET /contact?status=unread -> unread message count
Dashboard renders stat cards and quick navigation links
```

## Tasks

### Frontend
- [x] Create `front/src/hooks/useDashboard.ts`
  - [x] Calls `listProjectsAdmin` and `listMessages(idToken, "unread")` in parallel
  - [x] Returns `{ stats, unreadCount, loading, error }`
  - [x] Stats: `{ total, published, draft, featured }`
- [x] Update `front/src/pages/admin/DashboardPage.tsx` with full implementation
  - [x] Stat cards row: Total Projects, Published, Drafts, Featured, Unread Messages
  - [x] Quick links section: cards linking to admin sections
  - [x] Loading skeleton for stat cards
  - [x] Error state if API calls fail
- [x] Write component tests in `front/src/pages/admin/DashboardPage.test.tsx`

### Observability
- [x] No new Lambda alarms needed (uses existing projects and contact Lambdas)
- [ ] Verify dashboard loads correctly after login

### Documentation
- [x] Update `docs/project-settings/frontend.md` - document DashboardPage and `useDashboard` hook

## Acceptance Criteria
- [x] Dashboard renders stat cards with correct counts
- [x] Unread messages count is displayed
- [x] Quick links navigate to the correct admin sections
- [x] Loading skeleton is shown while data loads
- [x] Error state is shown if API calls fail
- [x] All component tests pass

## Verification
- [x] `npm.cmd run test -- --run` passed in `front/`.
- [x] `npm.cmd run build` passed in `front/`.
- [ ] Dev environment verification pending.

## Notes
- Stats are derived client-side from the full project list.
- The dashboard is the first page after login, so data loads in parallel.
- Unread message count uses the same contact endpoint as the admin navigation badge.

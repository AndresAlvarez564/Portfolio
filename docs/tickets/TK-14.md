# TK-14 | Configure CodeDeploy for Lambda with aliases and alarms

## Type
Infrastructure

## Priority
Medium

## Status
Done

## Branch
feature/tk-14-codedeploy → dev

## Merge Requirements
- [ ] All tasks completed and tested locally
- [ ] No secrets or credentials committed
- [ ] Tested in dev environment
- [ ] PR reviewed and approved

## Depends On
- TK-11 (Infrastructure: Lambda functions)

## Objective
Configure CodeDeploy for Lambda with aliases and CloudWatch alarms for safe deployments with automatic rollback.

## Proposed Scope
- Update `LambdasConstruct` to use Lambda versions and aliases
- Create CodeDeploy deployment groups for each Lambda
- Attach CloudWatch alarms for automatic rollback
- Configure deployment strategy: All-at-once for dev, Canary for prod

## Tasks
### Infrastructure
- [x] Update `infra/lib/constructs/lambdas.ts`
- [x] Create Lambda versions for each function
- [x] Create Lambda alias: `live` for each function
- [x] Create CodeDeploy deployment group for each Lambda
- [x] Configure deployment strategy:
  - Dev: All-at-once
  - Staging: Linear10PercentEvery1Minute
  - Prod: Canary10Percent5Minutes
- [x] Attach CloudWatch error alarms to deployment groups
- [x] Update API Gateway to use `live` alias instead of `$LATEST`
- [ ] Deploy to dev: `cdk deploy --profile portfolio-dev`

## Output Files
- Updated `infra/lib/constructs/lambdas.ts`

## Acceptance Criteria
- Lambda versions and aliases are created
- CodeDeploy deployment groups are configured
- CloudWatch alarms are attached for rollback
- API Gateway uses `live` alias

## Notes
- This enables safe deployments with automatic rollback on errors
- Practice CodeDeploy on small projects builds the habit for production

# TK-30 | Implement contact module - submit form, list messages, update status

## Type
Feature

## Priority
High

## Status
Ready for Review

## Branch
dev

## Merge Requirements
- [ ] All tasks completed and tested locally
- [x] No secrets or credentials committed
- [ ] Tested in dev environment
- [ ] PR reviewed and approved
- [x] No regressions in existing functionality

## Depends On
- TK-23 (Infrastructure: first deploy to dev)
- TK-32 (Feature: SQS email worker) - for email notification on submission

## Objective
Implement the contact Lambda routes, the public Contact page with form submission, and the admin Contact Messages management page.

## Tasks

### Backend
- [x] Implement `submit_contact(event)` in `lambdas/contact/routes/contact.py`
- [x] Check honeypot field and return `200 OK` silently when triggered
- [x] Validate required fields: `name`, `email`, `subject`, `message`
- [x] Validate `email` format
- [x] Validate `message` length: max 2000 characters
- [x] Generate UUID `messageId`, set `status=unread`, save to DynamoDB
- [x] Send message to SQS queue using `CONTACT_QUEUE_URL`
- [x] Implement `list_messages(event)` with optional `?status=` filter, require admin
- [x] Implement `get_message(event, message_id)`, require admin
- [x] Implement `update_message_status(event, message_id)`, require admin
- [x] Add `CONTACT_QUEUE_URL` to Lambda environment variables in CDK
- [x] Validate `status` is one of: `unread`, `read`, `archived`
- [x] Return correct HTTP status codes per section 23 of `config.md`
- [x] Write unit tests in `lambdas/contact/tests/test_contact.py` using `moto`

### Frontend
- [x] Create `front/src/services/contactService.ts`
- [x] Create `front/src/types/contact.ts`
- [x] Create `front/src/pages/public/ContactPage.tsx`
- [x] Create `front/src/pages/admin/MessagesPage.tsx`
- [x] Create `front/src/components/admin/MessageDetailModal.tsx`
- [x] Add routes to `App.tsx`
- [x] Add "Messages" link to `AdminLayout` nav with unread count badge

### Observability
- [x] CloudWatch error alarm exists for the contact Lambda through `LambdasConstruct`
- [x] CloudWatch duration alarm exists for the contact Lambda through `MonitoringConstruct`
- [x] Add structured log in `submit_contact`
- [x] Add structured warning log when honeypot is triggered
- [x] Add structured log in `update_message_status`
- [ ] Verify X-Ray traces appear for submit and list routes

### Documentation
- [x] Update `docs/project-settings/backend.md` - document contact routes, honeypot behavior, SQS integration
- [x] Update `docs/project-settings/frontend.md` - document ContactPage, honeypot field pattern, MessagesPage

## Acceptance Criteria
- [x] `POST /contact` saves the message and returns 200
- [x] `POST /contact` returns 200 silently when honeypot is triggered
- [x] `POST /contact` returns 400 for missing required fields or invalid email
- [x] `POST /contact` returns 400 if message exceeds 2000 characters
- [x] `GET /contact` returns messages sorted by date descending (admin only)
- [x] `GET /contact` supports `?status=` filter
- [x] `PATCH /contact/{id}` updates status and `gsi1sk`
- [x] All admin routes return 403 without admin group
- [x] Public Contact page submits form and shows success confirmation
- [x] Admin Messages page lists messages with status filter tabs
- [x] Message detail modal shows full content and status actions
- [ ] All unit tests pass
- [x] CloudWatch alarms exist for the contact Lambda

## Verification
- [x] `npm.cmd run build` passed in `front/`.
- [x] `npm.cmd run test -- --run` passed in `front/`.
- [x] `npm.cmd run build` passed in `infra/`.
- [ ] `pytest contact/tests/test_contact.py -q` blocked locally: `pytest` is not on PATH.
- [ ] Dev environment verification pending.

## Notes
- Honeypot field is `website` in the frontend; backend also accepts `honeypot` for compatibility.
- Contact messages are sent to SQS after DynamoDB persistence. The email worker is handled in TK-32.

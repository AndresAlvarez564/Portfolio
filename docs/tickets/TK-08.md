# TK-08 | Create Cognito User Pool and app client

## Type
Infrastructure

## Priority
High

## Status
Done

## Branch
feature/tk-08-cognito → dev

## Merge Requirements
- [ ] All tasks completed and tested locally
- [ ] No secrets or credentials committed
- [ ] Tested in dev environment
- [ ] PR reviewed and approved

## Depends On
- TK-04 (Security: roles and authorization rules)
- TK-07 (Infrastructure: CDK setup)

## Objective
Create the Cognito User Pool and app client for admin authentication.

## Proposed Scope
- Create `AuthConstruct` in `infra/lib/constructs/auth.ts`
- Create Cognito User Pool with email sign-in
- Create Cognito app client
- Create Cognito group: `admin`
- Disable self sign-up (admin-only access)

## Tasks
### Infrastructure
- [x] Create `infra/lib/constructs/auth.ts`
- [x] Create Cognito User Pool with naming: `<project-name>-<stage>-user-pool`
- [x] Configure sign-in: email (or username, depending on project)
- [x] Configure self sign-up based on project requirements
- [x] Create app client: `<project-name>-<stage>-client`
- [x] Configure auth flows: `USER_PASSWORD_AUTH`
- [x] Create Cognito groups based on TK-04 (e.g., admin, manager, viewer)
- [x] Export User Pool ID and Client ID for frontend config
- [ ] Deploy to dev: `cdk deploy --profile <project-name>-dev`

## Output Files
- `infra/lib/constructs/auth.ts`

## Acceptance Criteria
- Cognito User Pool is created in dev environment
- App client is created
- Admin group exists
- User Pool ID and Client ID are available for frontend configuration

## Notes
- Configure self sign-up based on project requirements (disabled for internal systems, enabled for public apps)
- Create initial admin user manually after deployment

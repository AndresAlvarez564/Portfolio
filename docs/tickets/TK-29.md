# TK-29 | Implement certifications module - CRUD

## Type
Feature

## Priority
High

## Status
Ready for Review

## Branch
dev

## Merge Requirements
- [ ] All tasks completed and tested locally
- [x] No secrets or credentials committed
- [ ] Tested in dev environment
- [ ] PR reviewed and approved
- [x] No regressions in existing functionality

## Depends On
- TK-23 (Infrastructure: first deploy to dev)
- TK-31 (Feature: media module) - for badge image upload

## Objective
Implement the certifications Lambda routes and the corresponding public Certifications page and admin Certifications management page, including badge image upload via the media module.

## Proposed Scope
- Backend: `GET /certifications` (public), `POST /certifications`, `PUT /certifications/:id`, `DELETE /certifications/:id` (write routes admin only)
- Frontend public: Certifications page with badge images and verification links
- Frontend admin: Certifications management page with CRUD and badge upload
- Observability: CloudWatch alarms and structured logs for the certifications Lambda

## Tasks

### Backend
- [x] Implement `list_certifications(event)` in `lambdas/certifications/routes/certifications.py`
  - Query GSI-1: `gsi1pk=CERTIFICATION`, sort descending by `gsi1sk` (most recent first)
  - Return all certifications
- [x] Implement `create_certification(event)` - validate body, generate UUID, PutItem, return 201
- [x] Implement `update_certification(event, certification_id)` - validate body, update item, require admin
- [x] Implement `delete_certification(event, certification_id)` - DeleteItem, require admin, return 204
- [x] Validate required fields: `name`, `issuer`, `issueDate`
- [x] Validate `issueDate` format: `YYYY-MM`
- [x] Validate `expirationDate` format if provided: `YYYY-MM`
- [x] Return correct HTTP status codes per section 23 of `config.md`
- [x] Write unit tests in `lambdas/certifications/tests/test_certifications.py` using `moto`

### Frontend
- [x] Create `front/src/services/certificationsService.ts`
- [x] Create `front/src/types/certification.ts` with `Certification` interface
- [x] Create `front/src/pages/public/CertificationsPage.tsx`
- [x] Create `front/src/pages/admin/CertificationsPage.tsx`
- [x] Create `front/src/components/admin/CertificationForm.tsx`
- [x] Add routes to `App.tsx`
- [x] Add "Certifications" link to `AdminLayout` nav

### Observability
- [x] CloudWatch error alarm exists for the certifications Lambda through `LambdasConstruct`
- [x] CloudWatch duration alarm exists for the certifications Lambda through `MonitoringConstruct`
- [x] Add structured log in `create_certification`
- [x] Add structured log in `delete_certification`
- [ ] Verify X-Ray traces appear for create and list routes

### Documentation
- [x] Update `docs/project-settings/backend.md` - document certifications routes and date format validation
- [x] Update `docs/project-settings/frontend.md` - document CertificationsPage and badge upload integration

## Acceptance Criteria
- [x] `GET /certifications` returns all certifications sorted by issue date descending
- [x] `POST /certifications` creates a certification and returns 201
- [x] `PUT /certifications/{id}` updates the certification
- [x] `DELETE /certifications/{id}` deletes the certification and returns 204
- [x] All write routes return 403 without admin group
- [x] `POST /certifications` returns 400 for invalid date format
- [x] Public Certifications page renders certification cards with badge images
- [x] Admin Certifications page supports CRUD with badge upload
- [ ] All unit tests pass
- [x] CloudWatch alarms exist for the certifications Lambda

## Verification
- [x] `npm.cmd run build` passed.
- [ ] `pytest certifications/tests/test_certifications.py -q` blocked locally: `pytest` is not on PATH.
- [ ] Dev environment verification pending.

## Notes
- Badge upload calls `POST /media/upload`, uploads the image to the returned signed URL, calls `POST /media/confirm`, then saves `badgeUrl` and `badgeS3Key` on the certification.
- `expirationDate` is optional.
- `verificationUrl` is optional; the public page renders a Verify button only when present.

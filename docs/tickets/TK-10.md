# TK-10 | Create API Gateway with Cognito authorizer

## Type
Infrastructure

## Priority
High

## Status
Open

## Branch
feature/tk-10-api-gateway → dev

## Merge Requirements
- [ ] All tasks completed and tested locally
- [ ] No secrets or credentials committed
- [ ] Tested in dev environment
- [ ] PR reviewed and approved

## Depends On
- TK-08 (Infrastructure: Cognito User Pool)
- TK-07 (Infrastructure: CDK setup)

## Objective
Create API Gateway REST API with Cognito authorizer for protected admin endpoints.

## Proposed Scope
- Create `ApiConstruct` in `infra/lib/constructs/api.ts`
- Create API Gateway REST API
- Create Cognito authorizer
- Configure CORS
- Create placeholder routes (will be connected to Lambdas in TK-11)

## Tasks
### Infrastructure
- [ ] Create `infra/lib/constructs/api.ts`
- [ ] Create API Gateway REST API: `<project-name>-<stage>-api`
- [ ] Create Cognito authorizer using User Pool from TK-08
- [ ] Configure CORS for frontend origin
- [ ] Enable CloudWatch logs
- [ ] Enable X-Ray tracing
- [ ] Create placeholder resources based on project domains
- [ ] Export API URL and API ID
- [ ] Deploy to dev: `cdk deploy --profile <project-name>-dev`

## Output Files
- `infra/lib/constructs/api.ts`

## Acceptance Criteria
- API Gateway is created in dev environment
- Cognito authorizer is configured
- CORS is enabled
- API URL is available for frontend configuration

## Notes
- Routes will be connected to Lambda functions in TK-11
- Public routes (contact form) will not use the authorizer
- Admin routes will require Cognito authorizer

# TK-11 | Create Lambda functions, IAM permissions, and test structure

## Type
Infrastructure

## Priority
High

## Status
Done

## Branch
feature/tk-11-lambdas → dev

## Merge Requirements
- [ ] All tasks completed and tested locally
- [ ] No secrets or credentials committed
- [ ] Tested in dev environment
- [ ] PR reviewed and approved

## Depends On
- TK-09 (Infrastructure: DynamoDB table)
- TK-10 (Infrastructure: API Gateway)
- TK-07 (Infrastructure: CDK setup)

## Objective
Create Lambda functions for each domain, configure IAM permissions, and set up test structure.

## Proposed Scope
- Create `LambdasConstruct` in `infra/lib/constructs/lambdas.ts`
- Create Lambda functions for each business domain identified in TK-02
- Configure environment variables (TABLE_NAME, STAGE, LOG_LEVEL)
- Grant DynamoDB permissions
- Connect Lambda functions to API Gateway routes
- Set up test structure in each Lambda folder

## Tasks
### Infrastructure
- [x] Create `infra/lib/constructs/lambdas.ts`
- [x] Create Lambda functions for each domain: `<project-name>-<stage>-<domain>` (Python 3.12)
- [x] Configure environment variables: TABLE_NAME, STAGE, LOG_LEVEL
- [x] Grant DynamoDB read/write permissions to all Lambdas
- [x] Configure memory: 256 MB for dev, 512 MB for prod
- [x] Configure timeout: 10 seconds for dev, 15 seconds for prod
- [x] Enable X-Ray tracing
- [x] Connect Lambdas to API Gateway routes
- [ ] Deploy to dev: `cdk deploy --profile <project-name>-dev`

### Backend
- [x] Create folder structure: `lambdas/<domain>/` for each business domain
- [x] Create `handler.py` in each Lambda folder
- [x] Create `routes/` folder in each Lambda
- [x] Create `utils/` folder in each Lambda
- [x] Create `tests/` folder in each Lambda
- [x] Add placeholder test file: `tests/test_example.py`

## Output Files
- `infra/lib/constructs/lambdas.ts`
- `lambdas/<domain>/handler.py` for each business domain
- Test structure in each Lambda folder

## Acceptance Criteria
- All Lambda functions are created in dev environment
- Lambda functions have DynamoDB permissions
- Lambda functions are connected to API Gateway
- Test structure exists in each Lambda folder

## Notes
- Lambda functions will be implemented in Phase 2 feature tickets
- This ticket only sets up the infrastructure and folder structure

# TK-09 | Create DynamoDB table with GSIs

## Type
Infrastructure

## Priority
High

## Status
Open

## Branch
feature/tk-09-dynamodb → dev

## Merge Requirements
- [ ] All tasks completed and tested locally
- [ ] No secrets or credentials committed
- [ ] Tested in dev environment
- [ ] PR reviewed and approved

## Depends On
- TK-03 (Data Model: DynamoDB table design)
- TK-07 (Infrastructure: CDK setup)

## Objective
Create the DynamoDB table with GSIs based on the data model designed in TK-03.

## Proposed Scope
- Create `DatabaseConstruct` in `infra/lib/constructs/database.ts`
- Create main table: `portfolio-<stage>-main`
- Configure partition key: `pk`, sort key: `sk`
- Create GSIs based on access patterns from TK-03
- Configure billing mode: PAY_PER_REQUEST
- Configure PITR for staging and prod

## Tasks
### Infrastructure
- [ ] Create `infra/lib/constructs/database.ts`
- [ ] Create DynamoDB table: `<project-name>-<stage>-main`
- [ ] Configure keys: `pk` (String), `sk` (String)
- [ ] Set billing mode: PAY_PER_REQUEST
- [ ] Create GSIs based on access patterns from TK-03
- [ ] Configure PITR: disabled for dev, enabled for staging/prod
- [ ] Configure removal policy: DESTROY for dev, RETAIN for staging/prod
- [ ] Export table name and ARN
- [ ] Deploy to dev: `cdk deploy --profile <project-name>-dev`

## Output Files
- `infra/lib/constructs/database.ts`

## Acceptance Criteria
- DynamoDB table is created in dev environment
- GSIs are created
- Table name is exported for Lambda functions
- Billing mode is PAY_PER_REQUEST

## Notes
- Use single-table design with generic pk/sk
- GSIs should match the access patterns documented in TK-03

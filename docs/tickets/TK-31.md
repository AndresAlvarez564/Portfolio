# TK-31 | Implement media module - pre-signed upload, confirm, list, delete

## Type
Feature

## Priority
High

## Status
Ready for Review

## Branch
dev

## Merge Requirements
- [ ] All tasks completed and tested locally
- [x] No secrets or credentials committed
- [ ] Tested in dev environment
- [ ] PR reviewed and approved
- [x] No regressions in existing functionality

## Depends On
- TK-23 (Infrastructure: first deploy to dev)

## Objective
Implement the media Lambda routes for S3 pre-signed URL upload, upload confirmation, media listing, and deletion. Also implement the reusable `MediaUpload` frontend component used by the certifications and profile modules.

## Tasks

### Backend
- [x] Implement `request_upload_url(event)` in `lambdas/media/routes/media.py`
- [x] Validate allowed content types
- [x] Validate allowed contexts
- [x] Generate S3 key: `media/<context>/<uuid>-<filename>`
- [x] Generate pre-signed PUT URL using `boto3` S3 client with 300-second expiry
- [x] Implement `confirm_upload(event)`
- [x] Verify the S3 object exists with `head_object`
- [x] Save MEDIA record to DynamoDB with GSI keys
- [x] If `context=cv`, update `PROFILE_SETTINGS.cvFileUrl` and `cvS3Key`
- [x] Implement `list_media(event)`
- [x] Implement `delete_media(event, media_id)`
- [x] Add `MEDIA_BUCKET_NAME` and `CLOUDFRONT_MEDIA_URL` env vars to media Lambda in CDK
- [x] CloudFront URL construction: `<CLOUDFRONT_MEDIA_URL>/<s3Key>`
- [x] Write unit tests in `lambdas/media/tests/test_media.py` using `moto`

### Frontend
- [x] Create `front/src/services/mediaService.ts`
- [x] Create `front/src/types/media.ts`
- [x] Create `front/src/components/MediaUpload.tsx`
- [x] Create `front/src/pages/admin/MediaLibraryPage.tsx`
- [x] Add `/admin/media` route to `App.tsx` (ProtectedRoute)
- [x] Add "Media" link to `AdminLayout` nav
- [x] Replace certification badge upload with reusable `MediaUpload`

### Observability
- [x] CloudWatch error alarm exists for the media Lambda through `LambdasConstruct`
- [x] CloudWatch duration alarm exists for the media Lambda through `MonitoringConstruct`
- [x] Add structured log in `request_upload_url`
- [x] Add structured log in `confirm_upload`
- [x] Add structured log in `delete_media`
- [ ] Verify X-Ray traces appear for upload and confirm routes

### Documentation
- [x] Update `docs/project-settings/backend.md` - document media routes, pre-signed URL flow, CV replacement behavior
- [x] Update `docs/project-settings/frontend.md` - document MediaUpload component and usage pattern

## Acceptance Criteria
- [x] `POST /media/upload` returns a pre-signed S3 URL
- [x] `POST /media/upload` returns 400 for disallowed content types
- [x] `POST /media/confirm` saves the MEDIA record and returns `cloudfrontUrl`
- [x] `POST /media/confirm` updates PROFILE_SETTINGS when `context=cv`
- [x] `GET /media` returns all media records (admin only)
- [x] `DELETE /media/{id}` deletes from both S3 and DynamoDB
- [x] All routes return 403 without admin group
- [x] `MediaUpload` component handles the full upload flow end-to-end
- [x] Media Library page lists and allows deletion of uploaded files
- [ ] All unit tests pass
- [x] CloudWatch alarms exist for the media Lambda

## Verification
- [x] `npm.cmd run build` passed in `front/`.
- [x] `npm.cmd run test -- --run` passed in `front/`.
- [x] `npm.cmd run build` passed in `infra/`.
- [ ] `pytest media/tests/test_media.py -q` blocked locally: `pytest` is not on PATH.
- [ ] Dev environment verification pending.

## Notes
- Pre-signed uploads use direct browser `PUT` requests with the selected file's `Content-Type`.
- `CLOUDFRONT_MEDIA_URL` is configured on the media Lambda from the media CloudFront distribution.
- CV upload replaces the profile settings reference but does not delete previous S3 objects.

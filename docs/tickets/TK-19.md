# TK-19 | Configure Amplify and Cognito authentication in the frontend

## Type
Infrastructure

## Priority
High

## Status
Done

## Branch
feature/tk-19-amplify-auth → dev

## Merge Requirements
- [ ] All tasks completed and tested locally
- [ ] No secrets or credentials committed
- [ ] Tested in dev environment
- [ ] PR reviewed and approved

## Depends On
- TK-08 (Infrastructure: Cognito User Pool)
- TK-18 (Infrastructure: React frontend scaffold)

## Objective
Configure AWS Amplify in the frontend and connect it to Cognito for authentication.

## Proposed Scope
- Install AWS Amplify v6
- Configure Amplify with Cognito User Pool and Client ID
- Create Amplify configuration file
- Test authentication flow (sign in, sign out)

## Tasks
### Frontend
- [x] Install AWS Amplify: `npm install aws-amplify`
- [x] Create `front/src/config/amplify.ts` with Cognito configuration
- [x] Configure Amplify in `main.tsx`
- [x] Add User Pool ID and Client ID from TK-08
- [x] Add API Gateway URL from TK-10
- [x] Test sign-in flow with a test admin user
- [x] Test sign-out flow
- [x] Verify token is stored in session

## Output Files
- `front/src/config/amplify.ts`

## Acceptance Criteria
- Amplify is configured with Cognito
- Sign-in flow works
- Sign-out flow works
- Token is stored and accessible

## Notes
- Create a test admin user in Cognito manually for testing
- Do not commit Cognito IDs — use environment variables or CDK outputs

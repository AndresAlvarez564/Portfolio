# TK-32 | Implement SQS email worker - contact form notification via SES

## Type
Feature

## Priority
Medium

## Status
Ready for Review

## Branch
dev

## Merge Requirements
- [ ] All tasks completed and tested locally
- [x] No secrets or credentials committed
- [ ] Tested in dev environment
- [ ] PR reviewed and approved
- [x] No regressions in existing functionality

## Depends On
- TK-30 (Feature: contact module - SQS message is sent from contact Lambda)

## Objective
Implement the SQS email worker Lambda that reads contact form submissions from the SQS queue and sends email notifications via SES.

## Tasks

### Infrastructure
- [x] Update `infra/lib/constructs/async-processing.ts`
- [x] Create SQS queue: `portfolio-<stage>-contact-queue`
- [x] Create DLQ: `portfolio-<stage>-contact-dlq`
- [x] Configure DLQ: `maxReceiveCount=3`
- [x] Configure queue visibility timeout: 30 seconds
- [x] Create email worker Lambda: `portfolio-<stage>-email-worker` (Python 3.12)
- [x] Add SQS event source to email worker Lambda
- [x] Grant SES send permissions to email worker Lambda IAM role
- [x] Export queue URL for contact Lambda environment variable
- [x] Update `infra/lib/constructs/lambdas.ts` with `CONTACT_QUEUE_URL` for contact Lambda
- [x] Add `SES_FROM_EMAIL` and `SES_TO_EMAIL` env vars to email worker Lambda
- [ ] Verify SES sender/recipient identity in AWS SES sandbox

### Backend
- [x] Create `lambdas/email_worker/` folder structure
- [x] Implement SQS `lambda_handler(event, context)`
- [x] Implement `process_record(record)` in `routes/email_worker.py`
- [x] Parse SQS message body as JSON
- [x] Extract `name`, `email`, `subject`, `message`, `messageId`
- [x] Send email via SES `send_email`
- [x] Raise exception on SES failure for SQS retry and DLQ handling
- [x] Write unit tests in `lambdas/email_worker/tests/test_email_worker.py` using `moto`

### Observability
- [x] Add CloudWatch alarm for `portfolio-dev-email-worker` Lambda errors
- [x] Add CloudWatch alarm for `portfolio-dev-email-worker` Lambda duration
- [x] Add CloudWatch alarm for DLQ depth
- [x] Add structured log in `process_record`
- [x] Add structured error log on SES failure
- [ ] Verify X-Ray traces appear for the email worker Lambda

### Documentation
- [x] Update `docs/project-settings/backend.md` - document email worker Lambda, SQS flow, DLQ behavior
- [x] Update `docs/project-settings/architecture.md` - confirm async email flow is implemented as designed

## Acceptance Criteria
- [x] SQS queue and DLQ are created in CDK
- [x] Email worker Lambda is triggered by SQS events
- [x] Email worker sends notification email via SES on contact form submission
- [x] On SES failure, message goes to DLQ after 3 retries
- [x] DLQ alarm fires when messages accumulate
- [x] CloudWatch alarms exist for the email worker Lambda
- [ ] All unit tests pass

## Verification
- [x] `npm.cmd run build` passed in `infra/`.
- [ ] `pytest email_worker/tests/test_email_worker.py -q` blocked locally: `pytest` is not on PATH.
- [ ] Dev environment verification pending.
- [ ] SES identity verification pending in AWS.

## Notes
- SES in dev is in sandbox mode; both sender and recipient addresses must be verified.
- Current configured placeholder is `andres@example.com` for `SES_FROM_EMAIL` and `SES_TO_EMAIL`.
- The email worker Lambda is not an HTTP Lambda.

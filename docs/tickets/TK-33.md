# TK-33 | Implement public HomePage - hero, featured projects, and section previews

## Type
Feature

## Priority
High

## Status
Ready for Review

## Branch
dev

## Merge Requirements
- [ ] All tasks completed and tested locally
- [x] No secrets or credentials committed
- [ ] Tested in dev environment
- [ ] PR reviewed and approved
- [x] No regressions in existing functionality

## Depends On
- TK-24 (Feature: profile module - GET /profile)
- TK-25 (Feature: projects module - GET /projects)
- TK-27 (Feature: experience module - GET /experience)
- TK-28 (Feature: skills module - GET /skills)
- TK-29 (Feature: certifications module - GET /certifications)

## Objective
Implement the public HomePage that serves as the main landing page for visitors. It displays the hero section with profile info, featured projects, and preview sections for experience, skills, and certifications.

## Tasks

### Frontend
- [x] Create `front/src/hooks/useHomePage.ts`
  - [x] Fires all API calls in parallel using independent settled requests
  - [x] Returns `{ profile, featuredProjects, experience, skills, certifications, loading, errors }`
  - [x] Each section has its own loading and error state
- [x] Update `front/src/pages/public/HomePage.tsx` with full implementation
  - [x] Hero section: name, title, summary, location, social links, CTA buttons
  - [x] Featured Projects section: grid of up to 3 featured project cards
  - [x] Experience preview section
  - [x] Skills preview section grouped by category
  - [x] Certifications preview section
  - [x] Contact CTA section
- [x] Each section handles its own loading skeleton and error fallback independently
- [x] CV download button only renders if `profile.cvFileUrl` is present
- [x] Featured projects section only renders if `featuredProjects.length > 0`
- [x] Write component tests in `front/src/pages/public/HomePage.test.tsx`

### Observability
- [x] No new Lambda alarms needed
- [x] Verify CloudFront serves the page correctly
- [x] Verify all API calls appear in X-Ray traces

### Documentation
- [x] Update `docs/project-settings/frontend.md` - document HomePage sections, `useHomePage` hook, and partial failure pattern

## Acceptance Criteria
- [x] Hero section renders profile name, title, summary, and social links
- [x] Featured projects section renders up to 3 featured project cards
- [x] Experience, skills, and certifications preview sections render correctly
- [x] CV download button is hidden when `cvFileUrl` is null
- [x] Featured projects section is hidden when no featured projects exist
- [x] A failed API call for one section does not break other sections
- [x] All component tests pass

## Verification
- [x] `npm.cmd run test -- --run` passed in `front/`.
- [x] `npm.cmd run build` passed in `front/`.
- [x] Dev environment verified: CloudFront returned 200 and homepage public APIs returned 200 with X-Ray traces.

## Notes
- The project backend currently exposes public `GET /projects`; the hook filters `featured = true` client-side for this homepage.
- The hero image `front/src/assets/hero.png` is used as the landing hero background.

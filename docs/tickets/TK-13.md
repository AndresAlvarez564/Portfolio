# TK-13 | Set up CodePipeline and CodeBuild

## Type
Infrastructure

## Priority
Medium

## Status
Done

## Branch
feature/tk-13-pipeline → dev

## Merge Requirements
- [ ] All tasks completed and tested locally
- [ ] No secrets or credentials committed
- [ ] Tested in dev environment
- [ ] PR reviewed and approved

## Depends On
- TK-07 (Infrastructure: CDK setup)
- TK-12 (Infrastructure: S3 and CloudFront)

## Objective
Set up CI/CD pipeline with CodePipeline and CodeBuild for automated deployments.

## Proposed Scope
- Create `PipelineStack` in `infra/lib/pipeline-stack.ts`
- Create CodePipeline with GitHub source
- Create CodeBuild project for build and deploy
- Configure pipeline stages: Source → Build → Deploy

## Tasks
### Infrastructure
- [x] Create `infra/lib/pipeline-stack.ts`
- [x] Create CodePipeline: `<project-name>-pipeline`
- [x] Configure GitHub source connection
- [x] Create CodeBuild project: `<project-name>-<stage>-build`
- [x] Configure buildspec.yml for:
  - Install dependencies (infra and frontend)
  - Build frontend: `npm run build`
  - CDK synth: `cdk synth`
  - Deploy AppStage
  - Upload frontend to S3
  - Invalidate CloudFront
- [x] Configure pipeline stages: Source → Build → Deploy
- [ ] Deploy PipelineStack manually: `cdk deploy PipelineStack --profile <project-name>-dev`

### Build Configuration
- [x] Create `buildspec.yml` in repository root

## Output Files
- `infra/lib/pipeline-stack.ts`
- `buildspec.yml`

## Acceptance Criteria
- CodePipeline is created
- Pipeline connects to GitHub repository
- CodeBuild project is configured
- Pipeline can build and deploy the application

## Notes
- PipelineStack is deployed manually once
- After that, the pipeline deploys automatically on git push
- This ticket can be done later if manual deployment is preferred initially

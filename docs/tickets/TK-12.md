# TK-12 | Create S3 bucket and CloudFront distribution

## Type
Infrastructure

## Priority
High

## Status
Done

## Branch
feature/tk-12-hosting → dev

## Merge Requirements
- [ ] All tasks completed and tested locally
- [ ] No secrets or credentials committed
- [ ] Tested in dev environment
- [ ] PR reviewed and approved

## Depends On
- TK-07 (Infrastructure: CDK setup)

## Objective
Create S3 bucket for frontend hosting and media storage, and CloudFront distribution for global delivery.

## Proposed Scope
- Create `FrontendHostingConstruct` in `infra/lib/constructs/frontend-hosting.ts`
- Create private S3 bucket for frontend
- Create S3 bucket for media (images, CV, screenshots)
- Create CloudFront distribution with Origin Access Control
- Configure CloudFront to serve React SPA (fallback to index.html)

## Tasks
### Infrastructure
- [x] Create `infra/lib/constructs/frontend-hosting.ts`
- [x] Create S3 bucket: `<project-name>-<stage>-frontend-<account-id>` (private)
- [x] Create S3 bucket for media if needed: `<project-name>-<stage>-media-<account-id>` (private)
- [x] Create CloudFront Origin Access Control for frontend bucket
- [x] Create CloudFront distribution for frontend
- [x] Configure error responses: 403 and 404 → /index.html (React Router fallback)
- [x] Configure default root object: index.html
- [x] Enable HTTPS only
- [x] Create CloudFront distribution for media bucket if needed
- [x] Export CloudFront URL and distribution ID
- [x] Export media bucket name if created
- [ ] Deploy to dev: `cdk deploy --profile <project-name>-dev`

## Output Files
- `infra/lib/constructs/frontend-hosting.ts`

## Acceptance Criteria
- S3 buckets are created in dev environment
- CloudFront distributions are created
- Frontend bucket is private (no public access)
- CloudFront serves the frontend correctly
- React Router fallback works (404 → index.html)

## Notes
- Frontend will be deployed in TK-18
- Media uploads will be implemented in Phase 2

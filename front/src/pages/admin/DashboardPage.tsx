// DashboardPage — admin dashboard.
// Full implementation in Phase 2 feature tickets.

import AdminLayout from "../../components/AdminLayout";

const DashboardPage = () => {
  return (
    <AdminLayout>
      <h1 className="text-2xl font-bold">Dashboard</h1>
      <p className="mt-4 text-gray-500">Admin panel — coming soon.</p>
    </AdminLayout>
  );
};

export default DashboardPage;
